info:
  name: quotesGetBondOrderBook
  type: http
  seq: 30
http:
  method: GET
  url: "{{baseUrl}}/market/quotes/bonds/{{symbol}}/order-book"
  params:
    - name: symbol
      value: "{{symbol}}"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
docs: |-
  Độ sâu bid/ask của 1 mã trái phiếu. Trả 404 `ORDER_BOOK_NOT_SUPPORTED` nếu mã không có depth.
