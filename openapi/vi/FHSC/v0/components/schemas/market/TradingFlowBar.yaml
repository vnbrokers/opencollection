type: object
description: Một phiên trong chuỗi dòng tiền.
required:
  - date
  - buy
  - sell
  - net
properties:
  date:
    type: string
    format: date
    description: Ngày của phiên (`YYYY-MM-DD`).
    example: "2026-05-27"
  buy:
    allOf:
      - $ref: "TradingMethodBreakdown.yaml"
    description: Chiều mua.
  sell:
    allOf:
      - $ref: "TradingMethodBreakdown.yaml"
    description: Chiều bán.
  net:
    allOf:
      - $ref: "TradingMethodBreakdown.yaml"
    description: Ròng = mua − bán. Âm nghĩa là bán ròng.
