info:
  name: quotesGetFundsCatalog
  type: http
  seq: 25
http:
  method: GET
  url: "{{baseUrl}}/market/quotes/funds"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
docs: |-
  Danh sách mã quỹ mở được hỗ trợ kèm metadata tĩnh. Gọi `/market/quotes/funds/{symbol}` để lấy dữ liệu realtime.
