info:
  name: performanceGetPnlToday
  type: http
  seq: 9
http:
  method: GET
  url: "{{baseUrl}}/trading/pnl-today/{userId}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
docs: |-
  Lãi / lỗ trong ngày của user, tổng hợp qua mọi tiểu khoản.
