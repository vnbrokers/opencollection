info:
  name: Danh sách hợp đồng phái sinh
  type: http
  seq: 3
  tags:
    - market
http:
  method: GET
  url: "{{baseUrl}}/api/v1/derivatives/contracts"
  auth: inherit
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/v1/derivatives/contracts"
      method: GET
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          {}
