info:
  name: ordersGetOrderBook
  type: http
  seq: 6
http:
  method: GET
  url: "{{baseUrl}}/trading/v1/accounts/{subAccountId}/order-book"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
docs: |-
  Sổ lệnh (orderbook) đầy đủ trong ngày (bao gồm cả lệnh từ broker-signal và
  HayBond).
