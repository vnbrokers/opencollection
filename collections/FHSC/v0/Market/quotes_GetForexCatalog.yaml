info:
  name: Danh mục forex
  type: http
  seq: 16
http:
  method: GET
  url: "{{baseUrl}}/market/quotes/forex"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
docs: |-
  Danh sách mã forex được hỗ trợ kèm metadata tĩnh. Gọi `/market/quotes/forex/{symbol}` để lấy dữ liệu realtime.
