info:
  name: Danh mục hàng hoá
  type: http
  seq: 22
http:
  method: GET
  url: "{{baseUrl}}/market/quotes/commodities"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
docs: |-
  Danh sách mã hàng hoá được hỗ trợ kèm metadata tĩnh. Gọi `/market/quotes/commodities/{symbol}` để lấy dữ liệu realtime.
