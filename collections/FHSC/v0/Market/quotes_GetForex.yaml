info:
  name: Giá realtime forex
  type: http
  seq: 17
http:
  method: GET
  url: "{{baseUrl}}/market/quotes/forex/{{symbol}}"
  params:
    - name: symbol
      value: "{{symbol}}"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
docs: |-
  Dữ liệu realtime của 1 mã forex. `symbol` không phân biệt hoa thường.
