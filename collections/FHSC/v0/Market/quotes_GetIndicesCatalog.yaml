info:
  name: quotesGetIndicesCatalog
  type: http
  seq: 13
http:
  method: GET
  url: "{{baseUrl}}/market/quotes/indices"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
docs: |-
  Danh sách mã chỉ số được hỗ trợ kèm metadata tĩnh. Gọi `/market/quotes/indices/{symbol}` để lấy dữ liệu realtime.
