allOf:
  - $ref: "../common/PageMeta.yaml"
  - type: object
    required:
      - results
    properties:
      results:
        type: array
        items:
          $ref: "MarketNewsItem.yaml"
        description: Tin tức của trang hiện tại, mới nhất trước.
