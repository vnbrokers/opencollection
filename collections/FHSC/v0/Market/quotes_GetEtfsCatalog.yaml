info:
  name: quotesGetEtfsCatalog
  type: http
  seq: 32
http:
  method: GET
  url: "{{baseUrl}}/market/quotes/etfs"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
docs: |-
  Danh sách mã ETF được hỗ trợ kèm metadata tĩnh. Gọi `/market/quotes/etfs/{symbol}` để lấy dữ liệu realtime.
